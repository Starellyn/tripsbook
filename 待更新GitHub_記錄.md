# 📌 待更新GitHub_記錄 — claude.ai ↔ Claude Code 常設工項清單

> **版本:v2(2026-07-06)** — 本檔自本版起**轉型為常設交辦通道**:
> claude.ai 負責「定義」(把需求寫成工項、指向規格章節),Claude Code 負責「實作」。
> 做完把該項移到「已完成」;實作中發現模版/渲染問題,記進「問題回報區」回流討論,
> **不要現場自行改版面**。
>
> repo:`Starellyn/tripsbook`　·　網址:`https://starellyn.github.io/tripsbook/`

---

## 一、待辦工項

### 工項:完成後的收尾(擁有者操作,非 VS Code)

- push 後用 GitHub Actions 分頁確認部署(勿只靠重新整理;驗證用無痕視窗)。
- 三份正本 v2.2 **重新上傳 claude.ai project knowledge**;日後 `構想書.md` 建立/更新後同樣重新上傳。
- **project instructions 加版本標記**(只有本人能改):在 instructions 開頭加一行——
  `本摘要對應三份正本 v2.2(2026-07-07);開場核對版本號,不一致先提醒。`
- 回 claude.ai 發起「tokyo-202610 構想書回填」對話(對應上方構想書工項的內容產出)。

---

## 二、問題回報區(Claude Code 實作時填)

> 發現模版/渲染的問題或疑義,記在這裡回流 claude.ai 討論,不自行改版面。

- **tokyo-202610-v2 收斂後去留**(2026-07-08):tokyo-202610 已收斂為單書 AB variant,新 index.html 不再有 sister-book 連結指向 v2;但 `tokyo-202610-v2/` 資料夾與書架 `BOOKS` 的 v2 卡片仍在。工項未授權刪除,待擁有者裁定:刪除 v2 資料夾＋書架卡片,或保留封存。
- **配套檔案清單與 repo 現況差異**(2026-07-07,工項 3-2 授權以 repo 為準):`Google同步_AppsScript程式碼.gs` 不在 repo(程式碼存於 Google Apps Script 端),清單條目已改為僅列 `Google同步_設定說明.md` 並註記;若 .gs 應入 repo 供版本管理,回流討論。

---

## 三、已完成(歸檔)

### 2026-07-08 批次:tokyo-202610 構想書回填＋收斂單書 AB variant

1. **構想書.md(v3.0)入 repo**:`tokyo-202610/構想書.md`,含 AB 版分歧、住宿專區、待查證清單。
2. **index.html 整份覆蓋**:claude.ai 端產出(模版+TRIP 合併、jsdom 驗證過),VS Code 照放不解讀。
   覆蓋後新版已無 sister-book 連結;`tokyo-202610-v2/` 與書架 v2 卡片是否移除,待擁有者裁定(見問題回報區)。

### 2026-07-07 批次:v2.2 文件修訂(工項 1–4)

1. **規劃準則.md → v2.2**:第二章補「構想書的存放」小節(入 repo、更新即重傳 project knowledge、續談以檔為準);定版列改引用 4-2;第四章改「轉換與定版 checklist」——4-1 轉換(補第 8 項反向追溯)、新增 4-2 定版 checklist 七項(含全書 NT$ 校對);第十一章末條改「轉草稿必跑 4-1、定版必跑 4-2」。
2. **CLAUDE.md → v2.2**:三階段 bullet 補構想書入 repo;專案結構圖改 27 章、加 `tokyo-202610/構想書.md`;工作流步驟 0 補構想書入 repo、步驟 4 改引用 4-2。
3. **旅遊行程書_規格書.md → v2.2**:配套檔案清單改現行路徑(舊命名條目移除;.gs 差異見問題回報區);第九章步驟 9 改引用規劃準則 4-2;第二十七章渲染規則補定版校對 cross-ref。
4. **本記錄檔整理**:tokyo-202610 兩工項(構想書回填、收斂單書 AB variant)獨立為待辦。

### 2026-07-06 小批次:規格 26 兩項疑義裁定落地

- num 英文序數重編(DAY ONE…FOURTEEN,超出數字 fallback;無 variants 用原值)、景點推薦分頁全版本共用——規格書 v2.1、模版查表實作,jsdom 24 項全過。
- 裁定原文「美食分頁」依回報脈絡判為「景點推薦分頁」筆誤,已依意圖寫入。

### 2026-07-06 批次:三階段流程改版(工項 1–4)

1. 三份正本 v2 入 repo 並 push。
2. 模版 variant 版本切換(規格 26):切換鈕、共用日＋當前版本日過濾、重編 Day 1…N、分歧日標籤、不存 localStorage;`meta.variants` 留空行為不變。
3. 模版匯率基準列(規格 27):draft 註「參考」、final 起「定版鎖定」,留白不顯示。
4. 模版 pending 空值防呆(規格 10):draft 且 pending 空時總覽頁灰字提示。

驗證:jsdom 行為測試 23 項全過。

### 2025 批次:書架與工具頁上線

書架首頁、`_shared` 共用資源、`_kakeibo` 記帳、`_warikan` 分帳、`_nikki` 日記、Google 同步(Apps Script)、家人意見按鈕、`tokyo-2026`→`tokyo-202610` 改名、規格書更新至二十一章。
(原始操作細節見 git 歷史;若此批仍有未上線項目,移回上方待辦。)

---

## 四、常備小抄(避免踩雷)

- 每本行程書、每個工具頁的檔名都用 `index.html`,網址才乾淨;網址一定帶資料夾名。
- 記帳/分帳/日記換新旅程要改 `KEY`、`SHEET_ID`、`LS_PREFIX`(帳本與勾選狀態才分開)。
- 改了 Apps Script 程式要「重新部署」才生效。
- push 後若沒更新,是瀏覽器快取 → GitHub Actions 確認部署,無痕視窗驗證。
